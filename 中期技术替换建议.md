# 中期技术替换建议

本文记录本轮 P0 修复之外的中期演进项。当前实现已经可以安全运行，以下替换应在达到触发条件后分批实施，避免为尚未出现的规模问题提前增加复杂度。

## 建议优先级

| 优先级 | 范围 | 当前情况 | 建议方案 | 启动条件 | 验收重点 |
| --- | --- | --- | --- | --- | --- |
| P1 | 高德接口治理 | 已使用 Spring `RestClient`，超时、错误映射和降级逻辑仍散落在 Provider 内 | 改为 Spring HTTP Interface，统一连接/读取超时、状态码映射、Micrometer 指标；连续出现限流或抖动后再引入 Resilience4j | 接入第二个地图供应商，或高德调用量/失败率需要单独治理 | 超时可控、调用指标完整、降级原因可追踪，不因重试放大高德流量 |
| P1 | 空间计算 | 电子围栏主要依赖 PostGIS，Java 侧仍有 WKT 和坐标列表转换 | 使用 JTS 作为 Java 统一几何模型，由 PostGIS 负责索引查询，JTS 负责入参合法性、自交检测和离线单测 | 围栏编辑、合并、缓冲区或复杂多边形规则增加 | 数据库与 Java 空间判断口径一致，非法多边形在入库前被拒绝 |
| P1 | API 契约 | 前后端 DTO 由 TypeScript 和 Java 分别维护 | 引入 springdoc 生成 OpenAPI，并用 Orval 等工具生成前端类型和请求客户端 | API 数量继续增长，或再次出现字段/枚举不一致 | CI 校验契约，生成代码不手工修改，错误模型和鉴权头统一 |
| P2 | 路线优化 | 目前最多 16 个任务，最近邻加 2-opt 足够且容易维护 | 任务规模或约束增加后评估 Timefold Solver，统一处理容量、班次、技能、时间窗和多运维人员分配 | 单次超过 30 个任务，或出现多人、多仓、时间窗、车辆容量约束 | 与现算法建立基准集，优化质量、耗时上限和降级路径可量化 |
| P2 | 报表存储 | 独立 Worker 已隔离查询与生成，文件暂存本地共享目录 | 部署多节点或上云时改为 S3 兼容对象存储，数据库只保存对象键、摘要和过期时间 | API/Worker 不再共享磁盘，或需要多实例和长期归档 | 分片上传、生命周期清理、临时下载地址、租户隔离和审计完整 |
| P2 | 遥测死信运维 | 已有限重试并写入 `<原主题>.DLT` | 增加 DLT 积压告警、管理端查看与按事件编号幂等重放；必要时再采用 Kafka Connect/专业消息治理平台 | 进入生产或 DLT 出现非零消息 | 不展示敏感原始数据，重放可审计，不跳过原有幂等和去重逻辑 |
| P3 | 作业图片 | 当前通过应用节点接收并保存原图 | 文件量明显增长后采用对象存储直传、异步缩略图和恶意文件扫描 | 日上传量、带宽或磁盘占用成为瓶颈 | 上传凭证短时有效，内容类型以文件检测为准，原图不可公开枚举 |

## 本轮已完成，不再列为替换项

- 车辆状态报表已接入持久化任务队列，由独立 `report-worker` 使用只读连接池流式生成；核心 API 只负责入队、查状态和流式下载。
- 遥测自动任务先于 Redis 缓存执行，相同时间戳可由 Kafka 重放补写缓存；持续失败进入 DLT。
- Android 已从 `addJavascriptInterface` 改为 AndroidX WebKit 同源 `WebMessageListener`，仅允许配置源的主框架调用白名单能力。
- 账号状态、角色或密码变化会撤销 Redis 中该用户的现有会话；运维任务、附件、路线和地理概览按城市/组织校验数据范围。

## 参考资料

- [Spring REST Clients 与 HTTP Interface](https://docs.spring.io/spring-framework/reference/integration/rest-clients.html)
- [LocationTech JTS 文档](https://locationtech.github.io/jts/)
- [springdoc-openapi](https://springdoc.org/)
- [AndroidX WebKit 发布记录](https://developer.android.com/jetpack/androidx/releases/webkit)
- [Timefold Solver 文档](https://docs.timefold.ai/timefold-solver/latest/)
