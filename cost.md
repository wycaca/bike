# 成本规划

本文按初创平台口径估算云资源. 价格以 2026-08-10 公开页面粗估, 实际采购以云厂商控制台, 地域, 活动价, 包年包月折扣为准.

## 估算原则

- 前期优先省钱和省运维, 不提前购买大规格托管服务.
- 后端保持无状态, 通过增加实例水平扩容.
- 有状态能力交给 Redis, PostgreSQL/TimescaleDB, Kafka 和对象存储.
- 轨迹是核心数据, 但原始轨迹保留天数和简化轨迹是否永久保存暂时待业务确认.
- 生产环境至少保留基础高可用: API 双实例, 数据库自动备份, Redis 高可用或主从, Kafka 托管或可快速恢复.

## 阶段配置

| 阶段 | 车辆规模 | 推荐资源 | 月成本粗估 |
| --- | ---: | --- | ---: |
| 验证期 | 0-500 辆 | 1 台 4核16G ECS, Docker Compose 部署 backend, PostgreSQL/TimescaleDB, Redis, 本地 Kafka 可暂缓 | 800-1500 元 |
| 试运营 | 500-5000 辆 | 2 台 4核16G ECS, 托管 RDS PostgreSQL/TimescaleDB 2核4G-4核8G, Redis 1-2GB, Kafka Serverless 基础规格, OSS | 4000-8000 元 |
| 单城规模 | 5000-20000 辆 | 3-4 台 4核16G 或 8核32G ECS, RDS 8核32G 起, Redis 4-8GB, Kafka Serverless 或包年实例, OSS 冷归档 | 1.2万-2.5万元 |
| 多城扩展 | 20000-100000 辆 | API/worker 分组水平扩容, TimescaleDB 独立高 IO, RDS 读副本, Redis 集群, Kafka 扩规格, OSS/离线分析 | 5万-15万元以上 |

## 验证期

目标是跑通真实业务闭环, 不追求完整高可用.

机器:

- 1 台 ECS, 4核16G, 100-200GB 云盘.
- 所有服务用 Docker Compose 部署.
- 带宽 5-10Mbps 起步, 管理端和 App H5 前期流量很小.

部署:

- backend 单体应用.
- PostgreSQL + TimescaleDB + PostGIS.
- Redis.
- Kafka 可用容器版, 也可以先用数据库表临时代替事件队列, 真车接入前再切 Kafka.

成本:

- ECS 约 600-1200 元/月, 活动价可能更低.
- 云盘, 快照, 域名, 证书, 少量 OSS 合计约 200-300 元/月.

## 试运营

目标是开始接真实车辆, 保留基础高可用.

机器:

- 2 台 ECS, 4核16G, 分别部署 API 和 worker, 或混部但使用负载均衡.
- 1 个托管 RDS PostgreSQL/TimescaleDB, 2核4G 起, 业务增长后升到 4核8G.
- Redis 1-2GB 主从或标准版.
- Kafka Serverless 基础规格.
- OSS 用于轨迹归档, 图片, 工单附件.

高可用:

- 后端服务无状态, 两台 ECS 挂到负载均衡.
- Redis 保存登录态, 验证码, 分布式锁, 车辆最新状态.
- 数据库开启自动备份和慢 SQL 监控.
- Kafka 消息保留 1-3 天, worker 可重复消费并保证幂等.

成本:

- ECS: 2 台 4核16G, 约 1200-2400 元/月.
- RDS: 约 300-1500 元/月, 取决于规格和折扣.
- Redis: 约 100-300 元/月.
- Kafka Serverless: 约 1000-3000 元/月起, 取决于预留带宽, 分区数和存储.
- 负载均衡, OSS, 日志, 监控: 约 500-1500 元/月.

## 单城规模

目标是支撑一个城市稳定运营.

机器:

- admin-api/app-api: 2-3 台 4核16G.
- iot-ingest: 2 台 4核16G 或 8核32G.
- worker: 2-4 台 4核16G 或 8核32G.
- RDS/TimescaleDB: 8核32G 起, 高 IO 云盘.
- Redis: 4-8GB, 高可用.
- Kafka: 托管实例, 分区按车辆上报 topic 和订单事件 topic 拆分.

成本:

- 计算: 4000-8000 元/月.
- 数据库: 4000-10000 元/月.
- Redis: 500-1500 元/月.
- Kafka: 3000-8000 元/月.
- 存储, 备份, 日志, 负载均衡, CDN: 1000-3000 元/月.

## 多城扩展

目标是 10 万辆级别, 成本主要来自轨迹写入, 数据库存储, Kafka 流量和日志.

机器:

- API 服务按流量横向扩容, 单实例保持 4核16G 或 8核32G.
- iot-ingest 独立扩容, 避免车辆上报影响管理端.
- worker 按 topic 分组扩容.
- TimescaleDB 独立高 IO 实例, 必要时按城市或时间做冷热拆分.
- Redis 集群保存车辆最新状态.
- Kafka 提升带宽和分区, 保留短周期原始消息.

成本控制:

- 空闲车辆降低采样频率.
- 原始轨迹保留天数待业务确认.
- 简化轨迹是否永久保存待业务确认.
- 超期原始轨迹归档到 OSS.
- 报表走小时级聚合表, 不直接扫明细轨迹.

## 待业务确认

- 原始轨迹保留多少天.
- 简化轨迹是否永久保存.
- 监管是否要求指定年限的数据留存.
- 是否需要按城市独立结算云资源成本.
- 是否需要等保, 审计, 灾备城市等合规投入.
